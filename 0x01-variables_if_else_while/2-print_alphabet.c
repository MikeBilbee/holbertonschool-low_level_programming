#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/**
 * main - Prints lowercase alphabet
 *
 * Return: always 0
 */
int main(void)
{
char z = 'a';
while (z <= 'z')
putchar(z);
z++;
putchar('\n');

return (0);
}
