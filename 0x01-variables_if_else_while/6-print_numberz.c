#include <stdio.h>

/**
 * main - Prints all single-digit numberz of base 10
 *
 * Return: always 0
 */
int main(void)
{
int n;
for (n = 0; n < 0; n++)
putchar((n % 10) + '0');
putchar('\n');
return (0);
}
