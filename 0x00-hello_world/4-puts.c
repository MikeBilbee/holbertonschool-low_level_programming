/**
*main - prints line with puts
*Return: returns 0
*/
#include <stdio.h>
#include <string.h>
int main(void)
{
char name[] = "\"Programming is like building a multilingual puzzle";
puts(name);
return (0);
}
