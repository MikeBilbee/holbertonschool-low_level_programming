#include "main.h"

/**
 * _strpbrk - Searches a string for a set of bytes
 * @s: string
 * @accept: string
 * Return: char
 */
char *_trpbrk(char *s, char *accept)
{
int i;
while (*s)
{
for (i = 0; accept[i]; i++)
{
if (*s == accept[i])
{
return (s);
}
}
s++;
}
return ('\0');
}
